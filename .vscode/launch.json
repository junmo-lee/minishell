{
	
	
	
	"version": "0.2.0",
	"configurations": [
		{
			"type": "lldb",
			"request": "launch",
			"name": "Debug",
			"program": "${workspaceFolder}/parse",
			"cwd": "${workspaceFolder}",
			"preLaunchTask": "Build Debug"
		}
	]
}